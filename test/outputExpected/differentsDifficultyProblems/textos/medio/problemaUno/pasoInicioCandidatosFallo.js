module.exports = {
	firstTime() {
		return 'No has acertado diciendo el conjunto de candidatos del paso de inicialización. Al ser el paso de inicialización, únicamente hemos seleccionado el nodo uno, por lo que el conjunto de candidatos es el resto. Prueba de nuevo a decir la solución al conjunto de candidatos del paso de inicialización.';
	}
};
